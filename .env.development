# Development environment variables

# In development, Next.js runs on port 3000, API server on port 8080
# But the frontend will automatically use window.location.origin
NEXT_PUBLIC_NODE_ENV=development

# Port configuration
PORT=3000
API_PORT=8080