# Production environment variables

# In production, everything runs on the same port
# Frontend will automatically use window.location.origin
NEXT_PUBLIC_NODE_ENV=production

# Single port for everything
PORT=8080